package people;

import java.util.Scanner;

public class CsvReader {
    Scanner scanner = new Scanner(System.in);
    //Left with setting up the file path as input and execute elvin's codes to read csv file
}